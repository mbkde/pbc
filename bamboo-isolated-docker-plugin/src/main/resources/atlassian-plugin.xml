<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="isolated-docker-plugin"/>

    <preBuildAction key="measureContainerStartup" name="Measure time to start Container Agent"
                  class="com.atlassian.buildeng.isolated.docker.CustomPreBuildActionImpl">
        <description>Measure the time between queue time and start time.</description>
        <resource type="freemarker" name="view" location="templates/plugin/jobConfiguration.ftl"/>
        <resource type="freemarker" name="edit" location="templates/plugin/jobConfiguration.ftl"/>
    </preBuildAction>

    <buildProcessor key="stopContainerAgent" name="Stop Container Agent" class="com.atlassian.buildeng.isolated.docker.StopDockerAgentBuildProcessor">
        <skipIfFailed>false</skipIfFailed>
    </buildProcessor>
    <component-import key="isolatedAgentService" interface="com.atlassian.buildeng.spi.isolated.docker.IsolatedAgentService"/>


    <bambooEventListener key="bambooEventListener" name="Bamboo Event Listener" class="com.atlassian.buildeng.isolated.docker.PreBuildQueuedEventListener">
        <description>Listens to events of Bamboo.</description>
    </bambooEventListener>
    <!-- REST resources -->
    <rest key="docker" path="/docker" version="1.0">
        <description>Provides the REST resource</description>
    </rest>
    <component-import key="xxxx" interface="com.atlassian.bandana.BandanaManager"/>
    
</atlassian-plugin>
