<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="Per-build Container API" plugins-version="2">
    <plugin-info>
        <description>Plugin with API for per-build container docker agents.</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    </plugin-info>
    <preBuildAction key="dummyPreBuildAction" name="Action performing a very complicated thing" class="com.atlassian.buildeng.spi.isolated.docker.DummyPreBuildAction">
        <description/>
    </preBuildAction>
</atlassian-plugin>
