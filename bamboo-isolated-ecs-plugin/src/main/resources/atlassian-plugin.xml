<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    </plugin-info>

    <component key="isolatedAgentService" class="com.atlassian.buildeng.ecs.ECSIsolatedAgentServiceImpl" public="true">
        <interface>com.atlassian.buildeng.spi.isolated.docker.IsolatedAgentService</interface>
    </component>

    <component key="ecsHandler" class="com.atlassian.buildeng.ecs.ECSHandler" public="true">
        <description>Handles AWS ECS interaction</description>
        <interface>com.atlassian.buildeng.ecs.ECSHandler</interface>
    </component>

</atlassian-plugin>
